/** npm 包名 */
export const PACKAGE_NAME = "ossput";

export const NPX_PACKAGE = PACKAGE_NAME;

export const NPX_SETUP = `npx ${NPX_PACKAGE} setup`;

export const NPX_DOCTOR = `npx ${NPX_PACKAGE} doctor`;
